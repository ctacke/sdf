using System;
using System.Collections;
using System.Collections.Specialized;

namespace OpenNETCF.Configuration
{
/*
This is going to be confusing alongside the existing ConfigurationSettings class. This is probably best left out until any reworking is done on the existing code to follow the new .NET 2.0 model

    /// <summary>
    /// Provides access to configuration files for client applications.
    /// </summary>
    public static class ConfigurationManager
    {
        //at some point the config system should be migrated to the 2.0 model. This is provided for v2.0 compatibility and merely calls back into configuration settings

        /// <summary>
        /// Gets the AppSettingsSection data for the current application's default configuration.
        /// </summary>
        public static NameValueCollection AppSettings
        {
            get
            {
                return ConfigurationSettings.AppSettings;
            }
        }

    }*/
}
